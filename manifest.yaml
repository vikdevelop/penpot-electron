app-id: com.github.vikdevelop.penpot
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: "21.08"
command: penpot.sh
sdk-extensions:
  - org.freedesktop.Sdk.Extension.node16
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --share=network
  - --filesystem=xdg-download
  - --device=dri
build-options:
  append-path: /usr/lib/sdk/node16/bin
  env:
    NPM_CONFIG_LOGLEVEL: info
modules:
  - name: penpot
    buildsystem: simple
    build-options:
        env:
          XDG_CACHE_HOME: /run/build/penpot/flatpak-node/cache
        build-args:
        - --share=network
    build-commands:
    - yarn
    - mkdir penpot
    - yarn electron-packager ./ penpot --platform=linux --arch=x64 --electron-version=15.4.1 --out=penpot/
    - mkdir /app/main
    - cp -R penpot/penpot-linux-x64/* /app/main
    - install -D -t /app/bin penpot.sh
    sources:
      - type: archive
        url: https://github.com/vikdevelop/penpot/archive/refs/tags/1.0.tar.gz
        sha256: 91800fd680c081271e309db2a3ff9a355c1f13a9f8a89df1ba39664db072a37d
      - type: script
        dest-filename: penpot.sh
        commands:
          - zypak-wrapper.sh /app/main/penpot "$@"
